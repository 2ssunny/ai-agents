interface:
  display_name: "Orchestrate Agents"
  short_description: "Coordinate explicitly requested parallel agent work"
  default_prompt: "Use $orchestrate to split this task into safe, independent agent workstreams."
